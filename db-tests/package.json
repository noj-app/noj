{
  "name": "noj-db-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Local Postgres tests for supabase-migration-lockdown-direct-writes.sql — RLS lockdown, cancel_appointment ownership, redeem_reward race-condition and search_path-hijack safety after its SECURITY DEFINER conversion.",
  "scripts": {
    "test": "node run_lockdown_tests.js"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
